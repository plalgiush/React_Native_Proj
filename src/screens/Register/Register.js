import SignUpForm from '../../components/Forms/SignUpForm.js/SignUpForm';

const Register = ({ navigation }) => {
  return (
    <SignUpForm navigation={navigation} />
  );
};

export default Register;
